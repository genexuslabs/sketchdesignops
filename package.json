{
  "name": "gxdesignops",
  "description": "",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "GxDesignOps",
    "manifest": "src/manifest.json",
    "main": "gxdesignops.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.7.0"
  },
  "author": "Gastón Milano <gmilano@genexus.com>",
  "dependencies": {
    "@skpm/child_process": "^0.4.2",
    "@skpm/dialog": "^0.4.0",
    "spawn-sync": "^2.0.0"
  }
}
